<template>
  <scroll-view class="content-scroll">
    <ul class="reslist">
      <repeat for="{{resList}}" key="index" index="index" item="item">
        <li id="{{item.id}}" bindtap="locateTo">
          <view class="list-item_pic">
            <image src="https://lg-kfc8r7cy-1252865942.cos.ap-shanghai.myqcloud.com/{{item.picid}}.jpg" mode="widthFix" class="list-picture"></image>
          </view>
          <view class="list-item_content">
            <view class="list-item_name">{{item.name}}</view>
            <view class="list-item_area">{{item.business}}</view>
            <view class="list-item_price">人均<view class="price_num">{{item.average_price}}</view>元</view>
          </view>
        </li>
      </repeat>
    </ul>
  </scroll-view>
</template>

<script>
  import wepy from 'wepy'
  export default class ResList extends wepy.component {
    props = {
      resList: {
        type: Array,
        default: []
      }
    }
    methods = {
      locateTo(e){
        var index = e.currentTarget.id
        wx.navigateTo({
          url: '/pages/detail?id=' + index
        })
      }
    }
  }  
</script>
<style>
.reslist{
  display: block;
  list-style: none;
  height: 100%;
  margin-left: 40rpx;
  margin-right: 40rpx;
}
.reslist li{
  overflow: hidden;
  display: block;
  font-size:16px;
  padding-top: 36rpx;
  padding-bottom: 36rpx;
  height:150rpx;
  border-bottom: 1px solid #f4f4f4;
}
.reslist li:last-child{
  border-bottom: none;
}
.list-item_pic{
  float: left;
  width: 200rpx;
  height: 150rpx;
}
.list-picture{
  width:200rpx;
  height:150rpx;
  border-radius:8rpx;
}
.list-item_content{
  margin-left: 230rpx;
  height: 150rpx;
  position: relative;
}

.list-item_name{
  font-size:18px;
  font-weight:bold;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  color:#000;
  padding: 0;
}

.list-item_area{
  position: relative;
  font-size:12px;
  display: inline-block;
  border:1px solid #e9e9e9;
  padding: 0 10rpx;
  font-weight: normal;
  color: #666;
  border-radius:6rpx;
}

.list-item_date{
  padding-top: 2px;
  font-size:12px;
  font-weight: normal;
  color: #6b7072;
}

.list-item_price{
  position: absolute;
  left: 0;
  bottom: 0;
  color:#fa5741;
  font-size:16px;
  padding: 0;
  margin: 0;
}

.price_num{
  display: inline;
  font-size:18px;
  font-weight: bold;
}
</style>
